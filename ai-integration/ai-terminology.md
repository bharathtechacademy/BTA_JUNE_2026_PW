# AI key terminology 

1. Conversational AI tools  
2. Copilot 
3. Coding assistant 
4. LLM 
5. MCP 
6. CLI 
7. RAG etc..


## Conversational AI tools 
Conversational AI tools are all about a set of AI-related web applications or desktop applications integrated internally with the AI model. These applications are going to allow us to chat with whatever the problem statement is that we have, and then it is going to provide you with one single response for the same. 

Examples: ChatGPT, Gemini, Claude, etc. 


USER -> PROMPT -> Chatgpt UI --> Authenticate -> Http Request -> PROMPT AS Request Body -> LLM -> Tokenization -> 


## LLM => Large language model 

1. DATA SOURCES (Internet, books, documents, databases, etc. )  -> 

2. Data processing  (- Cleaning the data,- Removing the duplicate data ,- Removing the junk data)

3. Tokenization ( Convert each and every data into tokens and assign some unique ID for each and every token. )

4. Model design  ( Now they are going to give unique weightage. )

5. Fine-tuning / Align 

6. It will be stored in the backend servers. (Vector databases )

7. LLM is ready

## Coding assistants 
Coding assistant is an IDE integrated with AI models. 

The best examples of coding assistants are Cursor, Windsurf, Codex, Anti Gravity, etc.


 ## MCP 

MCP is all about model context protocol. => 

MCP is a standard communication protocol that allows a model to connect with external tools and use them. 

Navigate to www.playwright.dev ==> await page.goto("www.playwright.dev")


## Agent 

An AI agent is a file that contains a set of instructions to intelligently understand the goal and make a decision based on the available information. It is also going to use tools, data sources, and applications to perform all the actions automatically, and finally, it is going to return the result to the user. 

## RAG  (Retrieval-augmented generation )

RAG is all about. It's one of the AI architectures that is going to combine the relevant data from the documents, databases, and websites, plus the LLM, which is already trained with huge content from the internet. 